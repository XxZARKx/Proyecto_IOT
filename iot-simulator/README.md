# IoT Simulator

Simulador Node.js que envia lecturas RFID, QR o sensores de peso al backend.

```bash
npm install
npm run simulate
npm run simulate:loop
```

Usa `.env.example` para configurar URL y credenciales.
