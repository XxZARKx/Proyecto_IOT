# Smart Inventory Web

Frontend React + Vite + Tailwind para StockIQ IoT.

## Ejecutar local

```bash
npm install
npm run dev
```

Configura `VITE_API_URL` si el backend no esta en `http://localhost:8080/api`.

Usuario demo: `admin@smartinventory.local` / `Password123`.
